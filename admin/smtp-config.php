<?php
/* =============================================================
   Foreningen Front Door — admin/smtp-config.php
   Configurare SMTP pentru trimiterea emailurilor din admin panel.

   Credențialele reale (SMTP_HOST, SMTP_USER, SMTP_PASS etc.) au
   fost mutate în /config.php (fișier ignorat de git — nu se
   urcă niciodată pe GitHub). Copiază config.sample.php în
   config.php și completează valorile reale.
   ============================================================= */

require_once __DIR__ . '/../config.php';
